import Link from "next/link";import XingyueyangHeader from "@/components/XingyueyangHeader";import FloatingAI from "@/components/FloatingAI";import {publicProducts} from "@/lib/products/server";
export const dynamic="force-dynamic";
export default async function Page(){const products=await publicProducts();return <main className="xy-glass-canvas min-h-screen"><XingyueyangHeader/><section className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><p className="text-xs tracking-[.2em] text-amber-600">PRODUCT SYSTEMS</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.05em]">产品中心</h1><p className="mt-5 max-w-2xl leading-7 text-slate-500">科学仪器与智能工业在线监测系统。设备资料由企业工作台统一维护。</p><div className="mt-12 grid gap-5 lg:grid-cols-3">{products.map((p:any)=><Link href={`/products/${p.slug}`} key={p.id} className="xy-glass-card group overflow-hidden rounded-[30px]"><div className="aspect-[4/3] bg-[#e9ecef]">{p.image_url?<img src={p.image_url} className="h-full w-full object-contain p-6 transition duration-500 group-hover:scale-[1.03]" alt={p.name}/>:<div className="grid h-full place-items-center text-sm text-slate-400">设备图片待上传</div>}</div><div className="p-6"><div className="text-xs font-semibold tracking-[.15em] text-amber-600">{p.model}</div><h2 className="mt-2 text-xl font-semibold">{p.name}</h2><p className="mt-3 text-sm leading-6 text-slate-500">{p.subtitle||p.description}</p><div className="mt-6 text-sm font-semibold">查看系统 →</div></div></Link>)}</div></section><FloatingAI/></main>}
