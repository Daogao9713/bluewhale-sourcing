import {NextResponse} from "next/server";import {publicCases} from "@/lib/cases/server";export const dynamic="force-dynamic";export async function GET(req:Request){try{const u=new URL(req.url);return NextResponse.json({success:true,cases:await publicCases(u.searchParams.get("featured")==="1")},{headers:{"Cache-Control":"no-store"}})}catch(e:unknown){return NextResponse.json({error:e instanceof Error?e.message:"Failed to load cases."},{status:500})}}