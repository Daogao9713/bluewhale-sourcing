import {NextResponse} from "next/server";import {db} from "@/lib/database/server";import {verifyWorkspaceKey} from "@/lib/workspace-auth";
export async function POST(req:Request){const a=verifyWorkspaceKey(req);if(!a.ok)return NextResponse.json({error:a.error},{status:a.status});try{const form=await req.formData();const file=form.get("file");if(!(file instanceof File))return NextResponse.json({error:"File required."},{status:400});if(file.size>10*1024*1024)return NextResponse.json({error:"Image must be under 10MB."},{status:400});if(!["image/jpeg","image/png","image/webp","image/gif"].includes(file.type))return NextResponse.json({error:"Unsupported image type."},{status:400});const ext=(file.name.split(".").pop()||"jpg").toLowerCase();const folder=String(form.get("folder")||"products").replace(/[^a-z0-9_-]/gi,"")||"products";const path=`${folder}/${Date.now()}-${crypto.randomUUID().slice(0,8)}.${ext}`;const {error}=await db().storage.from("xingyueyang-media").upload(path,file,{contentType:file.type,upsert:false});if(error)throw error;const {data}=db().storage.from("xingyueyang-media").getPublicUrl(path);return NextResponse.json({success:true,url:data.publicUrl,path})}catch(e:unknown){return NextResponse.json({error:e instanceof Error?e.message:"Upload failed."},{status:500})}}
