"use client";import {useEffect,useRef,useState} from "react";
export default function Reveal({children,className="",delay=0}:{children:React.ReactNode,className?:string,delay?:number}){const ref=useRef<HTMLDivElement>(null);const [on,setOn]=useState(false);useEffect(()=>{const el=ref.current;if(!el)return;const ob=new IntersectionObserver(([e])=>{if(e.isIntersecting){setOn(true);ob.disconnect()}},{threshold:.12});ob.observe(el);return()=>ob.disconnect()},[]);return <div ref={ref} style={{transitionDelay:`${delay}ms`}} className={`xy-reveal ${on?"is-visible":""} ${className}`}>{children}</div>}
